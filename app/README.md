# Babel Fish App

Flutter demo app for the Babel Fish fixture caption flow.

The app currently supports fixture-backed language pair selection for English, French, and Amharic demo data. Translated captions can be copied from the demo flow.

## Run

```sh
flutter pub get
flutter test
flutter run -d chrome
```
